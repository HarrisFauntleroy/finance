spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/alchemical-finance
    username:
    password:
  flyway:
    locations: classpath:db/migration,classpath:db/testdata
    baseline-on-migrate: true
    validate-on-migrate: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret: